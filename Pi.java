public class Pi{
public static void main(String[] args){
  int counter = 0;
  int exp = 3;
  double firstTerm = 0;
  double pi = 0;
  pi += 1/firstCounter;
  while (counter < 1000){
    int firstCounter = 2;
    while (firstCounter < exp){
      sum += 1/firstCounter
    }
    pi += (1/firstCounter)*(1/ exp)*(Math.pow(0.5,exp));
    exp += 2;
    counter --;
    }
  System.out.println(pi);
  }
}
